# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database Configuration (Supabase PostgreSQL)
# Transaction Mode - Use for Next.js app (with PgBouncer connection pooling)
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true

# Session Mode - Use for Drizzle migrations (direct connection)
DIRECT_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

# App Configuration
NEXT_PUBLIC_APP_URL=https://creator-ops-eta.vercel.app

# Optional: For production
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# Groq AI
GROQ_API_KEY=your-groq-api-key
# Optional: Override extraction model (default: llama-3.1-8b-instant)
# GROQ_EXTRACTION_MODEL=llama-3.1-8b-instant
# Optional: Cooldown after quota/rate-limit hit (milliseconds, default: 1800000)
# AI_QUOTA_DISABLE_MS=1800000

# Redis for BullMQ jobs (Upstash Redis URL works here)
# UPSTASH_REDIS_URL=rediss://default:password@<your-upstash-host>:6379
# REDIS_URL=redis://localhost:6379

# Resend email (reminder delivery)
# Free tier note: 100 emails/day
# RESEND_API_KEY=re_xxx
# RESEND_FROM_EMAIL=CreatorOps <noreply@yourdomain.com>
# Optional: route all reminder emails to one inbox during testing
# REMINDER_TEST_RECIPIENT_EMAIL=ramya.pasupuleti3@gmail.com

# Web Push (VAPID)
# Generate with: npx web-push generate-vapid-keys --json
# VAPID_PUBLIC_KEY=...
# VAPID_PRIVATE_KEY=...
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=...
# VAPID_SUBJECT=mailto:support@yourdomain.com

# Vercel Cron protection (used by /api/cron/reminders)
# CRON_SECRET=replace-with-a-long-random-string
